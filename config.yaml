gpswox:
  url: "https://drsecuritygps.com/api"
  api_hash: "${GPSWOX_API_HASH}"
  polling_interval_ms: 30000

sutran:
  url: "https://ws03.sutran.gob.pe/api/v2.0"
  access_token: "${SUTRAN_ACCESS_TOKEN}"
  timeout_ms: 10000

app:
  port: 3000
  log_level: "info"
